# OPG434 continuation package

verdict: candidate_only
delivery: local files; not a GitHub submission

The authoritative remote state at the latest reads is:
- main: d8220b87d903640d1abd2d835aba2f01a32f23dc;
- sole research coordination Issue: 3;
- C14 observed merged as PR 17;
- C15 PR 18 open at 7d278210fe9f751b260b6f5a10584273375a468c, with all three required checks successful;
- this conversation's exposed GitHub actions do not include writes or merge.

## New material

The self-contained layered pentagon proof is
research/artifacts/candidates/opg434-a01-layered-pentagon-repair-20260907-v1.md.
It removes the bridge limitation of the observed C15 construction and replaces
a one-vertex hole by an induced pentagon hole. It supplies both a full positive
map and an obstructed exterior map, proves strong connectivity and girth five,
and forces an arbitrarily long path of changed labels in every completion.
It remains a paper candidate, not a proof-kernel result or a root resolution.

The source note distinguishes this statement from frozen full homomorphisms
under one-vertex reconfiguration. The PR 18 review and Issue 3 comment are
UNSUBMITTED drafts. The JSON checkpoint states the actual transport limitation.

## Collision and stale-base protection

Do not overlay any older C14 packet template. A locally prepared C14 template
was superseded by the real C14 merge and is intentionally not in this archive.
Do not create a second C15 packet, Issue, or PR. Finish or observe completion
of the actual existing PR 18 first.

The preparation_base_revision in the proof records when the local candidate
was frozen; it is not authorization to reuse that commit as a future packet's
base. A future new packet must bind a fresh real main and a real candidate
branch. No future branch or PR identifier has been invented.

All files in this archive are under candidate/source-note paths. Only new
material should be transported when writes become available; existing truth
records, schemas, Harness, workflows, scripts, Evidence and Result remain
untouched. The manifest contains byte SHA-256 checksums, not verifier receipts.

best_verified_candidate=none; best_verified_result=none.
Open obligations: obligation:opg434-five-transversals-equivalence;
obligation:opg434-root. This package is not a completion declaration.
