# Layered family audit: statement faithfulness and execution boundary

verdict: candidate_only
base_revision: 08a8bce1a183c8feaa6fe139cc90b0cc58cfac6d

Only current main bytes were used as mathematical inputs. No new literature
claim, C10 recovery, attachment inventory or external status inference is made.
The primary input is
research/artifacts/candidates/opg434-a01-layered-pentagon-repair-20260907-v1.md,
SHA-256 c0e9115a80eb09f692fb3c83a52c8002766219b1154970ad21cc29442a5ba9aa,
Git blob 47194660177840c621a279f8147ac911edbc3e0f, 14694 bytes.
The local text's Git object digest was recomputed and matched the freshly
read main object. This is byte integrity only, not a mathematical check.

## Precisely preserved statements

The canonical problem asks for a five-edge assignment on every triangle-free
cubic graph, with bipartite complements of individual color classes. C01
relates this fixed-assignment condition to hitting every odd cycle.
The layered claim is about target-vertex maps and arbitrary simultaneous
repairs of a given exterior map. It is not an edge-edit lower bound, not a
claim about all exterior maps, and not a root counterexample: an explicit
total map is part of the witness. Target index i=0,...,4 decodes to the
contract color i+1. On every edge not of this color, coordinate i flips,
so the coordinate directly supplies the required deleted-color bipartition.
No reverse equivalence is needed to demonstrate these positive graphs.

C14's source reduction selects a suitable exterior map existentially.
C15's earlier negative repair family has bridges. C16 keeps unresolved
four-/five-port belt frontiers. The present strong-connectivity audit
neither settles those frontiers nor invalidates existential state selection.
These three main candidates were read as scope comparisons; their unverified
reduction lemmas are not premises of this audit's cut proof.

## Exact and abstract certificates must not be confused

The 20-vertex h=1 pinned core has exact root messages A and exact projected
central domains U(A). Enumeration of U(A)^5 covers every possible core
central tuple. The five-variable parity CNF is a SOUND abstraction, not a
claimed bijective encoding of the 100-vertex graph. Its contradiction is
transferred by the fully stated message and bipartition bridge. The JSON
resolution proof was hand-derived; no SAT solver or proof checker ran.

## Present execution status

Current WEB_CHANNEL_PROFILE.json reports command_execution=false. The
computation skill's prescribed scripts/compute_plan.py was not found on
this main (404). No installed prover/solver version, successful execution,
exit code, CPU usage, enumeration count or mutation-run result is inferred.
The companion Python source received syntax parsing only, without import,
execution, graph generation or certificate replay. Its numerical expectations
are derived in the paper audit and remain predicted until an authorized run.
Future runs must record actual versions, limits, exits and byte hashes.
This audit does not attest to a central launch receipt that is not present
among its read inputs, and it does not create or alter an authorization.

First remaining requested check: authorized h=1 full-object replay and the
five mutation controls. The general proof remains a paper candidate; a
trusted statement-faithfulness/assurance process is still needed. Neither
CI nor candidate merge changes that status.
